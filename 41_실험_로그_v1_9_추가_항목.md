## ChatGPT Custom GPT 이식 주석

- 아래의 Claude·Sonnet·Opus·API 표기는 **과거 운영 이력**이다.
- 현재 이식본에서는 같은 역할을 Custom GPT 내부 단계, Actions, 또는 사람 검토로 해석한다.
- 로그 자체는 append-only 성격을 유지한다.


# 실험 로그 v1.9 추가 항목

**계층**: L5 운영 로그
**상위 참조**: L0 `00_파이프라인_v3_최상위_구조.md`
**역할**: 파이프라인 v1.9 실험 로그 + 본 세션 7회 위반 + v2.0 마이그레이션
**L0 참조 헤더 추가**: 2026.04.23

# 기존 실험 로그(v1.6·v1.7·v1.8 기록) 끝에 이어붙일 증분 블록
# 2026.04.20

---

## [조건부 Ops] v1.9 — 2026.04.20 — Phase 1a→1b→1c 단일 트리거 통합 + 디버깅 예외 라우팅

### 배경

**트리거 단순화 필요성 (핵심 근거):**
v1.8까지 "ㄱㄱ(1a) → 1a 보고 → ㄱ(1b+1c)" 2단 트리거 운용. 실사용 이력 누적 결과:
- 형이 1a 보고 후 중단 판단을 내린 경우 **0회**
- 1a 독립 출력이 실질적으로 빈 체크포인트로 기능
- 매 사이클마다 형이 "ㄱ" 입력하는 수작업 반복
- 결과: 체크포인트 수는 많지만 실효성 0

**디버깅 용도 분리 필요성:**
동시에 형이 가끔 컨텍스트 수집 결과만 확인하고 싶은 경우 존재. 예: 델타 검증, 전체 분야 분포 확인. 이 용도를 위한 별도 진입 경로 필요.

**구조적 정합화 판정:**
수정 프로토콜 3회 반복 원칙 면제 적용. 근거:
- 기존 합의(Phase 1a→1b→1c 자동 흐름)의 정합화 패치
- 신규 규칙 도입이 아니라 실사용 이력이 이미 증명한 불필요 중단점 제거
- 형이 이미 실질적으로 이 흐름으로 운용 중

### 변경 내역

**1. Phase 1a 독립 출력 제거**
- 수집 요약은 1c 출력 헤더로 흡수
- Phase 1a는 내부 앵커로만 유지, 형 노출 없음
- 일반 경로에서 독립 출력 없음

**2. ㄱㄱ 단일 트리거화**
- ㄱㄱ 한 번으로 Phase 1a → 1b → 1c 연속 실행
- 중간 사용자 확인 없음
- 형 체크포인트 6개 → 5개 (2b hold 미발생 시 4개)

**3. "ㄱ" 중간 트리거 제거**
- 메인 흐름에서 "ㄱ / 계속 / 진행 / 빈 메시지" 예비 트리거로 격하
- 일반 상황 단독 입력 시: "메인 흐름에서 활성 트리거 아님. 디버깅 정지 상태가 아닙니다" 한 줄 응답

**4. 디버깅 예외 라우팅 신설**
- "컨텍스트만" / "1a까지" / "1a만" 입력 시 Phase 1a만 실행 후 정지
- 독립 출력 허용. **1c 헤더와 동일 수준 정보량**으로 출력 (컨텍스트 검증 목적 충족)
- 이후 "ㄱ" 입력 시 1a 결과 재사용하여 1b+1c 재개. 1a 재실행 금지.

**5. 디버깅 재개 경로 1c 헤더 중복 방지**
- 디버깅 모드에서 1a 독립 출력 후 "ㄱ"로 재개 시, 1c 출력 헤더에서 컨텍스트 요약 생략
- "📋 제작지시서 N건 점검 완료 (컨텍스트 헤더 생략 — 디버깅 모드에서 이미 출력됨)" 한 줄로 대체
- 동일 정보 2회 노출 방지

**6. 인스타 단독 주문 트리거 명시**
- "인 민3" 같은 주문 입력만으로 별도 ㄱㄱ 트리거 없이 옥토리타스 트랙 자동 진입
- ㄱㄱ은 "트랙 1 전용" 신호로 확정

**7. 복합 주문 트랙 순서 고정**
- "블 민3 + 인 민3 ㄱㄱ" 복합 주문 시 트랙 1 Phase 1a→1b→1c 먼저 완주
- 1c 출력 완료 후 트랙 2 옥토리타스 Stage 1 자동 진입
- 병렬 실행 금지 (토큰 한도 초과 위험 + 결과물 혼재 방지)

**8. 롤백 조건 평탄화**
- v1.6~v1.9 전체 유효 조건 14개를 v1.9 시점에 통합 명시
- 버전 간 중첩 참조 제거 ("v1.N 기존 조건 모두 유효" 체인 해소)
- 운영 시 단일 목록 참조로 추적 부담 감소

### 운용점검 반영 (v1.9 초안 → v1.9 확정)

초안 배포 전 전수 점검 수행. 치명 3건 + 경미 3건 식별 후 전면 재작성:

**치명 1 — 복합 주문 시 트랙 동시 실행 프로토콜 부재**
- 초안: "블/홈 ㄱㄱ 즉시 실행" + "인스타 주문 시 옥토리타스 즉시 소환" 두 경로 동시 요구 → 순서 불확정
- 수정: 상단 지시문 + 복합 주문 처리 섹션 + 옥토리타스 라우팅 섹션에 "트랙 1 먼저 완주 후 트랙 2 진입" 명시

**치명 2 — 인스타 단독 주문 트리거 규정 부재**
- 초안: ㄱㄱ이 "트랙 1만"이라고 박혀있어 인스타에는 쓸 수 없는데 옥토리타스 진입 트리거 명시 없음
- 수정: 옥토리타스 트랙 라우팅 조항에 "[A] 인스타 단독 주문 트리거 = 주문 입력만으로 자동 진입" 명시

**치명 3 — 디버깅 재개 후 1c 헤더 중복 출력 우려**
- 초안: 1c 출력 규칙이 조건 없이 "1a 수집 요약을 헤더로 흡수"하도록 되어 있어 디버깅 경유 시 같은 정보 2회 노출
- 수정: 1c 출력 규칙을 [A] 일반 경로 / [B] 디버깅 경로로 분리

**경미 1 — "ㄱ" 예비 트리거 일반 상황 해석 모호**
- 초안: "예비 트리거 — 메인 흐름에서 미사용"만 명시, 실수 입력 시 처리 미정의
- 수정: 예외 라우팅 테이블에 "일반 상황 단독 입력 시 한 줄 응답" 행 추가

**경미 2 — 디버깅 출력과 1c 헤더 정보량 역전**
- 초안: 디버깅 모드 출력이 간소한데 1c 헤더가 더 풍부 → 디버깅 목적(컨텍스트 검증)과 역행
- 수정: 디버깅 모드 출력을 1c 헤더와 동일 수준 정보량(판례 DB·자기잠식·BL↔HP 5요소 포함)으로 통일

**경미 3 — 롤백 조건 중첩 참조**
- 초안: "v1.8 기존 롤백 조건 모두 유효" → v1.8은 "v1.7 …" 식 중첩으로 운영 부담
- 수정: 실험 로그에 "현재 유효한 롤백 조건 통합 목록 (v1.9 기준)" 섹션 신설, 14개 조건 평탄화

### 운용점검 2차 반영 (1차 배포 후 5회 심층 점검)

1차 반영 완료 후 추가로 5개 관점(교차 정합성 / 개념 정의 / 실행 시나리오 / 엣지 케이스 / 누락·승계) 독립 점검. 경미 5건 추가 반영:

**[A] 대체 사이클 상한 별개 명시**
- 점검 발견: 규칙 349행 "Phase 1c 대체 사이클과 동일 상한 공유"가 해석 모호 (각자 1회 vs 합쳐서 1회)
- 수정: "별개의 상한이며, 각각 1회씩 허용" 명시. 해석 여지 제거.

**[B] Phase 2 "수정 지시" 세부 규정**
- 점검 발견: "해당 항목만 수정 재출력"만 명시, 주체·재점검 경로·2b 진입 조건 부재. v1.8 승계 기존 모호함.
- 수정: 2-way 분기 명시
  - 2-a. 단순 수정(오타·표현·필드 미세 조정) → 오케스트레이터 직접 수정, 1b 재호출 없음
  - 2-b. 구조 수정(차별화·키워드·구성안·핵심결론 변경) → 1b 리서처 재호출 + 1c 재점검
  - 분류 애매 시 역질문. 수정 후 Phase 2 유지, "오키 확정" 시에만 2b 진입.

**[C] 2회째 2b hold 후 무한 루프 차단**
- 점검 발견: 349행 "재진입 후에도 2b hold 시 자동 루프 없이 형 판단 대기"가 "자동 루프"만 막음. 형이 반복 "수정" 입력 시 수동 루프 가능.
- 수정: 2b→1b 재진입 건별 최대 1회 하드 상한. 2회째 hold 이후 "수정" 입력 자동 거부. "그대로 진행" 또는 "폐기"만 허용. 예외 라우팅 테이블에 1회·2회 hold 분기 행 추가.

**[D] 디버깅 정지 상태 중 새 주문 처리 규정**
- 점검 발견: Phase 1a 디버깅 예외가 "ㄱ 입력 시 재개"만 규정, 중간에 분야·채널 다른 새 주문 들어올 때 처리 부재.
- 수정: 새 주문의 트리거 형식에 따라 모드 전환 명시
  - "ㄱㄱ" → 메인 흐름 전환 (1a→1b→1c 연속)
  - "1a까지" 계열 → 디버깅 모드 유지 (1a만 재실행 후 정지)

**[E] "로드" 개념 재서술**
- 점검 발견: 상단 지시문 "Phase 3 진입 시점부터 Phase 4 재작성 완료 시점까지 로드"가 메인 GPT 세션 지속 로드로 읽힘. 실제는 건별 API 세션에 전문 주입.
- 수정: "오케스트레이터가 참조 권한 활성화 후 각 API 호출에 전문 포함 방식으로 주입. 메인 세션 지속 로드 아님" 명시.

### v1.9 최종 집계 (1차 + 2차 통합)

- 치명 3건 (복합 주문 순서 / 인스타 단독 트리거 / 디버깅 헤더 중복) + 경미 3건 (예비 트리거 일반 해석 / 디버깅 정보량 역전 / 롤백 중첩) = **1차 6건**
- 대체 상한 별개 / Phase 2 수정 지시 분기 / 2회째 hold 루프 차단 / 디버깅 중 새 주문 / "로드" 재서술 = **2차 5건**
- **합계 11건 전수 반영 완료**

### 관찰 포인트 (v1.9 운용 중 추적)

1. 복합 주문 시 트랙 1→트랙 2 순차 실행이 단일 응답 토큰 한도를 초과하는지
2. 디버깅 트리거 오발동으로 의도치 않은 1a 정지가 발생하는지
3. 일반 상황에서 "ㄱ" 단독 입력이 실제로 얼마나 자주 발생하는지 (빈도 높으면 트리거 재설계)
4. 1c 디버깅 경로 헤더 생략이 혼란을 주는지

### 동시 갱신 문서

- 파일 접근 규칙 v1.5 → **v1.6**
  - Phase 1a 소환 규칙: 독립 출력 제거
  - Phase 1b 트리거: "1a 완료 즉시 자동 실행"
  - 디버깅 트리거 소환 규칙 신설
  - 인스타 단독 주문 / 복합 주문 트랙 순서 명시
  - 예외 라우팅 소환 규칙 (디버깅 / 예비 트리거 일반) 신설

- 파이프라인 본체 v1.8 → **v1.9**

- FRWRITER v2.7 / 검토지침 v2.7 / 법률상담 문체 보정 규칙 v1.0 / 옥토리타스 차터 v3 / 판례 DB — **수정 없음**

### 롤백 조건 (본체 "현재 유효한 롤백 조건 통합 목록"으로 이동, v1.9 신설분만 여기 재기)

1. ㄱㄱ 단일 트리거 후 컨텍스트 드리프트로 1c 단계에서 분야/채널 오인식이 2회 이상 반복 → 2단 트리거 복원
2. 연속 실행이 단일 응답 토큰 한도 초과 유발 → 1b 단독 출력 분리 검토
3. 디버깅 트리거("컨텍스트만" / "1a까지" / "1a만") 오발동으로 의도치 않은 1a 정지 반복 → 트리거 문자열 재조정
4. 복합 주문 시 트랙 1→트랙 2 순차 실행이 단일 응답 한도 초과 유발 → 트랙 2 실행 시점 별도 트리거("옥 진행" 등) 도입 검토

v1.6~v1.8 승계 롤백 조건은 본체 「현재 유효한 롤백 조건 통합 목록」 참조.

---

끝.
---

## [2026.04.22 추가] 이번 세션 누적 위반 패턴 (7회차)

### 위반 회차별 기록

| 회차 | 위반 내용 | 태그 분류 |
|---|---|---|
| 1 | 샘플링 편향으로 "전부 비어있음" 단정 (field_missing = field_empty 오판) | 샘플 편향 |
| 2 | "80자/Round" 근거 없는 실측 주장 | 추정 포장 |
| 3 | "×1.5" 관례 근거 없이 박음 | 규칙 8 부재 |
| 4 | 캘린더 이벤트로 문서 성격 자료 무단 생성 | 월권 |
| 5 | "주 25건 넘기 전에 박아야" 근거 없는 임계치 | 규칙 8 위반 |
| 6 | "시연" 프레이밍으로 자기검증 면책 시도 | 규칙 우회 |
| 7 | "지시가 있으니 진행" 자기 지시 내러티브 | 규칙 10-3 |

### 구조적 한계 인정

본 세션 7회 위반 전부 프로젝트 지식 로드 상태에서 발생 [실측 — 본 세션 내 관찰]. 문서 박기만으로는 세션 간 attention 지속 보장 불가.

### v2.0 마이그레이션 기록

- 계기: 2026.04.22 마스터 DB(`5p6ala647q843e2c10eu3iiue0`) 덮어쓰기 사고
- 원인: Actions/OAuth silent truncation + GPT 인지 실패 + update_event 덮어쓰기 구조
- 대응: Round 이벤트 분할(append-only) + `{DB_END_SENTINEL_v2}` 검증 + 연도별 분할 로드맵 사전 설정

### 재발 방지 장치 (확정 구조 4개 항목)

본 실험 로그 후속 갱신은 통합본 v1 Part D 확정 구조 기반.


---

## [신규 부록] 메타 규칙 준수 선언 (2026.04.22 추가)

본 파일은 파이프라인 운영 규칙 통합본 v1 (이하 "통합본")의 규칙 8~11, 사전 계산 체크리스트, 확정 구조 4개 항목 적용 대상임을 명시한다.

### 1. 태그 체계 의무 (통합본 규칙 8)
본 파일 내 수치·평가·판정에 다음 4종 태그 중 하나 부착 의무. 태그 뒤 근거 한 줄(출처·시점·수치 중 최소 하나) 포함.
- [실측] 직접 측정·관찰, 시점 명시 필수
- [추정] 관찰 기반 외삽·추론
- [원칙] 이론·규칙·논리 근거
- [관례] 업계·실무 통용 기준, 구체 출처 명시 시에만

동어반복 근거 무효 (통합본 규칙 9): "실무에서 씀", "보통 그럼", "일반적", "흔히", "대부분", "많이 쓰임" 등만으로 구성된 근거는 무효 처리.

### 2. 금지 사항 4종 (통합본 규칙 10)
1. 단일 답안 단언 금지
2. 근거 없는 추천 금지
3. 임의 설계 결정 금지 — 사용자 결정 영역에 임의 결정 금지, 옵션만 제시
4. 자기 검증·자기 지시 내러티브 금지 — "자가검토 완료", "이상 없음", "시연", "진행하겠다", "효율적일 것 같다" 등 일체

### 3. 양식 빈 칸 금지 (통합본 규칙 11)
표 기반 출력 시 모든 컬럼 필수. 해당 없음인 경우 "해당 없음 [근거]" 명시, 빈 셀·생략 금지.

### 4. 확정 구조 4개 항목 참조
- 검증 주체: 작성 = Custom GPT 생성 단계 / 검토 = Custom GPT 내부 R1·R2·R3 순차 단계
- 양식 강제: 프롬프트 문구 (주) + JSON schema (보조)
- 세션 attention: 프로젝트 지식 + self-reminder + Custom Instructions 3중
- 반려 처리: Custom GPT 내부 최종 검토 단계 또는 사람 검토자가 수용

### 5. 위반 처리
위반 지적 시 즉시 반려 수용. 변명·프레이밍 우회 금지. 재제출 시 위반 지점만 수정. 8회차 누적 시 해당 작업 산출물 전체 폐기.

끝.

## [조건부 Ops] v1.10 — 2026.04.25 — 정합화 패치 14건

### 배경
BL-084~090 행정 배치 운영 과정에서 태그 층별 합산, 대표 태그 변형 중복, 법률효과 단정, 로컬 본질성, Schema B와 본체 기준 충돌, 공개 본문 메타 태그 노출 위험, 정본 지정 혼재 위험이 확인됐다.

### 변경 내역
1. Schema B 채널별 태그·FAQ 조건 분리 + Phase 1b 태그 전략·Phase 1c #6·Phase 2b #8 동시 분리
2. `[실측/추정/원칙/관례]` 태그 체계의 내부/공개 본문 적용 범위 분리 + 검토지침 기존 조항 해석 변경
3. 실행 정본 지정 및 v1.9 레거시 본체 격리
4. Phase 1c #5 키워드-법률효과 연동
5. Phase 1c #8 단정·과장 범위 확장 — 분야 무관 공통
6. Phase 1c #9 로컬 본질성 질 기준 명시
7. Phase 1c #10 전체 태그 줄 변형 중복 점검
8. Phase 2b #1 우선 재검증 5항목 + 갱신 트리거
9. 부록 A 게이트 6 신설 — 수치·법률효과 확정근거 차단
10. 부록 A 게이트 7 신설 — 로컬 본질성 차단
11. 부록 A 게이트 3 확장 — 전체 태그 줄 변형 중복
12. Phase 1b 자가검토 8블록 [2][5][6][8] 정합 보강
13. 옥토리타스 복합 주문 병렬 표현 제거
14. Linter 또는 최종 문서 점검 — 버전명·교차참조·Schema B·부록 A 정합 확인

### 미세 조정
- Phase 1c #8의 단정·과장 기준에서 행정 한정을 제거하고 분야 무관 공통으로 적용한다.
- Phase 1c #9 로컬 본질성은 OR 카운트가 아니라 실질적 영향의 질로 판단한다.
- Phase 2b #1 갱신 트리거의 `한 배치 3건+` 임계값은 유지하되, 배치 분할 시 분할 전 원배치 기준으로 이어서 판단한다.

### 귀속 정리
- Schema B 채널별 태그·FAQ 조건 분리는 v1.10 공식 변경 내역 #1에 포함한다.
- `[실측/추정/원칙/관례]` 적용 범위 분리는 v1.10 공식 변경 내역 #2에 포함한다.
- 위 두 항목은 별도 15·16번이 아니다.
- v1.10 총량은 14건으로 유지한다.

### 롤백 조건
1. 부록 A 게이트 6으로 인해 수치·기간이 없는 일반 글까지 과도하게 보류되는 경우 2회 이상 → 게이트 6 적용 범위 재조정
2. 부록 A 게이트 7로 인해 자연스러운 로컬 예시까지 과도하게 보류되는 경우 2회 이상 → 게이트 7 적용 위치를 제목·태그 한정으로 축소
3. Phase 2b 우선 재검증 5항목이 3배치 연속 실효 없이 통과만 반복 → 우선순위 목록 재평가
4. Schema B 채널별 분리 후 검토 출력 혼선 발생 → schema 설명부 보강

### 관찰 포인트
1. BL-087 유형 수치·감면 키워드가 #5→#14로 제대로 연동되는지
2. BL-086 유형 처분성 단정이 #13/#11/#14에서 잡히는지
3. 로컬 본질성 기준이 과도하게 작동하지 않는지
4. 홈페이지 태그 3~4개 케이스가 Schema B에서 정상 통과하는지
5. 공개 본문에 `[실측/추정/원칙/관례]` 태그가 노출되지 않는지

## [조건부 Ops] v1.10.1 — 2026.04.25 — AI 엔티티 설계 압축 + 자가검토 안전장치

### 배경
GPT/AI 검색에서 조국환 변호사팀이 단순 법률 답변 출처를 넘어 지역·분야·쟁점 엔티티로 인식되도록, 리서처 제작지시서 단계의 설계를 보강한다.

v1.10.1은 v1.10을 대체하지 않는다. v1.10 정합화 14건 적용 직후 R3 체크를 거친 뒤, 같은 배포 사이클 안에서 붙는 보조 패치다.

### 변경 내역
1. 키워드 전략에 답변형 AI 질의 / 팀 탐색형 질의 추가
2. AI 참조 친화 구간 강도 보강
3. 팀 엔티티 앵커 블록 신설
4. 유입 가설 블록 신설
5. A판정 추가 조건 한 줄 삽입
6. Phase 1c #15·#16 신설
7. Phase 2b #1·#7 문구 정합화
8. 자가검토 8블록 [2][4][5][8] 보강

### 성격 구분
- 1~7번: AI 엔티티 설계 본패치
- 8번: 출력 누락과 검수 누락을 막기 위한 안전장치
- Schema B 변경 없음
- 부록 A 보강은 즉시 반영하지 않고, 충돌 반복 시 후속 확장

### 롤백 조건
1. 제작지시서가 과도하게 길어져 Phase 1c 출력 누락이 2회 이상 발생
2. 홈페이지 본문에 사무소 홍보 문장이 섞여 발행 검수에서 2회 이상 보류
3. 팀 탐색형 질의가 모든 글에 기계적으로 붙어 로컬 끼워넣기 문제가 2회 이상 발생
4. AI 답변 후보문이 핵심결론과 충돌해 Phase 1c #16 revise가 3회 이상 반복

### 관찰 포인트
- 홈페이지 글에서 LLM 리퍼러 발생 여부
- 답변형 질의와 팀 탐색형 질의가 실제로 분리되는지
- 팀 엔티티 앵커가 본문 홍보가 아니라 허브·내부링크 설계로 작동하는지
- Phase 1c #15~#16이 누락을 잡는지
